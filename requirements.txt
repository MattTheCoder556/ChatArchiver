# Playwright drives a real Mozilla Firefox build (no Chrome/Chromium, no Google binary).
# The bundled Firefox is installed separately with:  python -m playwright install firefox
playwright

# Cookie-handoff export (cookie_export.py) — the path that gets past Cloudflare without
# any automated browser: reads your everyday browser's session and replays the APIs with a
# browser-impersonating TLS client.
curl_cffi
browser_cookie3

# Sun Valley ttk theme — the modern Windows 11 look for the desktop window.
sv-ttk

# Renders/resizes the bundled provider + app logos for the window (tk only shows bitmaps).
Pillow

# System-tray icon — lets the app keep running with its window closed (Windows
# notification area / Linux status tray). Reopen or quit from the tray menu.
pystray
